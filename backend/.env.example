APP_BIND=127.0.0.1:3001
DATABASE_URL=sqlite://data/investment.db
AUTH_REQUIRED=true
# Required when AUTH_REQUIRED=false so the core service only accepts gateway traffic.
INTERNAL_API_TOKEN=
RUST_LOG=personal_investment_backend=debug,tower_http=info
